export { default } from "next-auth/middleware";

export const config = {
  // matcher: [
  //   // 보호할 경로 지정
  //   "/((?!login|api/auth|_next/static|_next/image|favicon.ico).*)",
  // ],
  matcher: [],
};
